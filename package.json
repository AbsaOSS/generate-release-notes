{
  "name": "generate-release-notes_initial",
  "version": "1.0.0",
  "dependencies": {
    "@octokit/core": "^5.0.2",
    "@octokit/rest": "^20.0.2"
  },
  "devDependencies": {
    "@actions/core": "^1.10.1",
    "@vercel/ncc": "^0.33.0"
  },
  "scripts": {
    "build": "ncc build scripts/generate-release-notes.js -o dist"
  }
}
